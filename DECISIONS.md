# Decisions

One line per decision. Append-only, newest last.

Cheap by design: a bullet here, not an ADR. Promote an entry to a full ADR in
[`docs/adr/`](docs/adr/) only once the decision proves durable.

Format: `YYYY-MM-DD: <decision>, because <reason>. Revisit if <condition>. [→ link]`

---

- 2026-07-11: Capture decisions as one-line entries here by default, not as ADRs, because ADR-0001's own trade-off ("needs the discipline to write") is exactly what happened: `docs/adr/` has held only ADR-0001 since 2026-05-22 and no decision was ever recorded after it. Revisit if entries here also go unwritten. → [docs/adr/0001](docs/adr/0001-record-architecture-decisions.md)
- 2026-07-11: Scope Agent API threads, runs, crons, checkpoints, and persistent stores by authenticated token subject and reserve shared config mutations for an explicit admin scope, because authentication alone does not prevent cross-user resource access. Revisit if authorization moves to database RLS or one immutable admin subject.
- 2026-07-11: Serialize same-thread runs with owner-scoped FIFO queues (in-process locally; Redis lease and heartbeat under ARQ), because LangGraph checkpoints must not execute concurrently or cross owners. Revisit if dispatch moves to a transactional database queue.
- 2026-07-11: Keep external link previews public but bound origin work with request, concurrency, response-size, and cache limits, because hover previews must work without sign-in while arbitrary outbound fetches need abuse containment. Revisit if traffic requires a shared rate-limit store.
- 2026-07-11: Treat the TypeScript wiki verifier as the canonical provenance contract and keep the dependency-free Python verifier behaviorally equivalent, because local skill runs and CI must accept and reject the same sources. Revisit if both environments adopt one shared verifier runtime.
- 2026-07-11: Backfill pre-authorization Agent data at startup only when one Auth.js owner is explicit or unambiguous and legacy writers are stopped, because guessing ownership or racing old writes can expose or hide historical conversations. Revisit if migrations move to an external deployment job.
- 2026-07-26: Agents may change `web/` and `agent/` on a feature branch through a pull request, never by a direct commit to `main`, because the blanket ban in ADR-0002 meant agent-authored code never reached the CI that already guards those paths. Revisit if a PR merges on red CI or review latency blocks the work. → [docs/adr/0003](docs/adr/0003-agent-code-changes-via-pr.md)
- 2026-07-26: Give `docs/` documents frontmatter with `when_to_read`, and split investigations into `docs/research/` from decisions in `docs/adr/`, because a decision record that carries its comparison detail stops being short enough to read. Dropped `covers[]`/`doc_class` from the upstream schema, since no doc-sync automation exists here to keep them true. Revisit if doc-sync lands in CI. → [docs/conventions/frontmatter.md](docs/conventions/frontmatter.md)
- 2026-07-26: Keep `DECISIONS.md` as one append-only file and give it a `merge=union` driver, because the single-file property is what makes "read it before changing an established pattern" one cheap read, and parallel agent branches appending at the tail conflict on every PR otherwise. Revisit if entries outgrow one screenful of scanning.
- 2026-07-26: Deploy the existing agent to Cloud Run before any restack, because nothing is deployed anywhere (no Dockerfile, no `langgraph.json`, and the documented dev command cannot run) and a migration with no running baseline has nothing to verify against. Revisit if deploying proves harder than migrating. → [docs/plans/rag-restack.md](docs/plans/rag-restack.md)
- 2026-07-26: Do not adopt Aegra yet; gate it on a checkpoint-identity migration spike, an explicit call on run serialization, and a deepagents smoke test, because Aegra would replace 4,700 LOC that already works and it does not serialize same-thread runs at all. Revisit when the spike passes or Aegra reaches 1.0. → [docs/adr/0004](docs/adr/0004-keep-custom-agent-protocol-server.md)
- 2026-07-26: Repair the `@langchain/react` v1 API drift in `chat-section.tsx` rather than migrating to assistant-ui, because the stated motivation (LangGraph compatibility) already exists and the broken features are a stale type cast, not a framework problem. Revisit after using the repaired UI, on the narrower case of branch switching and a thread list. → [docs/adr/0005](docs/adr/0005-repair-chat-ui-defer-assistant-ui.md)
- 2026-07-26: Open the chatbot to anonymous visitors using Turnstile-gated tokens with random `anon:<uuid4>` subjects, because every downstream filter reads only the token subject so per-visitor isolation costs one file. Rollout is gated on fixing two live content leaks and adding rate limiting first. This amends the 2026-07-11 scoping entry, whose wording assumed every subject is an authenticated Auth.js user. Revisit if abuse outruns per-subject limits. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-07-26: Add the code interpreter with `langchain-quickjs` (official LangChain package on PyPI; Python host, JS-only WASM sandbox, in-process), not a container sandbox, because the threats that dominate a container - metadata-server SSRF, env-var exfiltration, egress - are structurally absent rather than configured away. Keep `subagents=False` initially: `task()` bypasses the tool-calling path so HITL approval is not enforced per dispatch. Revisit if wasmtime CVEs or token fan-out become the binding cost.
- 2026-07-26: Adopt Aegra 0.9.24 and delete the ~4,700-LOC hand-rolled Agent Protocol server, discarding existing agent data rather than migrating it, because the checkpoint-identity migration was the only real blocker and 25 threads of test data are disposable. Reverses the earlier same-day entry. Revisit if Aegra goes quiet or traffic needs more than one instance. → [docs/adr/0004](docs/adr/0004-adopt-aegra.md)
- 2026-07-26: Rebuild the chat on `@assistant-ui/react` 0.14.27 with the `@assistant-ui/react-langgraph` adapter (not `react-langchain`, which targets LangChain.js runnables), accepting the loss of branch switching, because the backend is now an Agent Protocol server and that adapter makes exactly one SDK call with everything else under our control. Reverses the earlier same-day entry. → [docs/adr/0005](docs/adr/0005-adopt-assistant-ui.md)
- 2026-07-26: Amends the 2026-07-11 run-serialization entry: Aegra parses `multitask_strategy` and never reads it, so owner-scoped FIFO with Redis leases is lost. The stand-in is an in-process busy set correct only at Cloud Run `--max-instances 1`. Revisit when Aegra PR #462 merges.
- 2026-07-26: Treat Aegra's unconditional `WHERE user_id = identity` SQL predicate as the authorization boundary, never the `@auth.on.*` handlers, because handlers are not dispatched on `/runs/stream`, `/runs/wait`, the stateless run variants, or `/threads/{id}/events` - exactly the path the frontend uses. Revisit if PR #385 merges. → [docs/research/aegra-native-stack.md](docs/research/aegra-native-stack.md)
- 2026-07-26: Keep Postgres on Neon and split it into two free projects (agent, auth) in a US region co-located with Cloud Run, because Supabase's floor is $25/month from day one for features this project does not use and its 7-day inactivity pause is a silent outage for a public chatbot. Revisit if sustained compute passes ~0.32 CU. → [docs/adr/0007](docs/adr/0007-postgres-on-neon-split-projects.md)
- 2026-07-26: The agent's purpose is to implement and compare retrieval methods, with the blog as the corpus; answering blog questions well is a side effect. Recorded because the goal was unstated and produced a confidently wrong recommendation to collapse the retrieval surface. Method breadth is a feature, and a correct baseline is a blocker rather than a bug. Revisit if the project becomes a product. → [docs/adr/0008](docs/adr/0008-chatbot-is-a-rag-evaluation-testbed.md)
- 2026-07-26: Track retrieval methods in a living registry at `docs/reference/retrieval-methods.md` rather than as ADRs, because the catalogue changes continuously and losing methods are worth keeping with their result. Revisit if an individual method's outcome becomes load-bearing enough to need its own ADR.
- 2026-07-26: Sequence the restack as basic-chat-first (Aegra spike, agent rebuild, deploy, assistant-ui) before the evaluation harness and before going public, because a working chat is the only thing that proves the whole infrastructure chain connects, while the harness is offline batch that does not need it. Revisit if the chat path blocks longer than the harness would have taken. → [docs/plans/rag-restack.md](docs/plans/rag-restack.md)
- 2026-07-26: Put the retriever Protocol in `agent/src/agent/retrieval/protocol.py` (stdlib only) rather than in the eval workspace, so the chat and the harness physically cannot drift onto different interfaces, and split `eval/` off as a separate uv workspace member on the line of servable-vs-not so torch never reaches the Cloud Run image.
- 2026-07-26: Make the draft/published boundary a build-time fact - a published-only mirror at `agent/.index/posts/` as the container's only content root - rather than a runtime predicate, because three code paths each had to remember to apply it and two forgot while the third failed open. Revisit if content ever needs to be served live rather than baked.
- 2026-07-26: Keep the chatbot open for any visitor to test without an account, but deploy the pre-hardening Cloud Run service behind fail-closed owner auth and enable anonymous subjects only after isolation, concurrency, retention, and spend gates pass, because an unlisted Aegra URL without registered auth is fail-open rather than private. Revisit if the public testbed becomes owner-only or P5 moves before the first deploy. → [docs/plans/rag-restack.md](docs/plans/rag-restack.md)
- 2026-07-26: Evaluate QuickJS and dynamic subagents as separate agent-capability axes before combining them, with one shared atomic run budget across model, tool, and nested `task` dispatches and no QuickJS-to-`task` bridge, because code execution does not create subagents and dispatching `task()` from inside an eval bypasses the normal approval/call-limit path. Revisit after the 2×2 capability experiment or when deepagents makes nested dispatch enforce the same middleware gates. → [docs/plans/rag-restack.md](docs/plans/rag-restack.md)
- 2026-07-26: Use Aegra's latest verified Agent Protocol v2 thread-centric streaming surface in production, with a typed assistant-ui transport over content-block and lifecycle events rather than the legacy `runs.stream` adapter, because Aegra 0.9.24 ships AP v2 streaming via native v3 and the testbed should exercise the current protocol rather than merely retain backward compatibility. Revisit when assistant-ui ships a native AP v2 adapter or the upstream streaming schema changes incompatibly. → [docs/plans/rag-restack.md](docs/plans/rag-restack.md)
- 2026-07-26: Build every agent delivery attempt from the exact git SHA through GitHub Actions using Workload Identity Federation, push a never-reused run/attempt tag, pass only the same-job-resolved Artifact Registry digest, gate traffic on protocol/security/browser smoke tests, and roll back by Cloud Run revision traffic rather than rebuilding, because the baked corpus and runtime must be traceable to the same reviewed source state without trusting a pre-existing tag. Revisit if deployment moves away from Cloud Run or Vercel. → [docs/plans/rag-restack.md](docs/plans/rag-restack.md)
- 2026-07-26: Treat Aegra 0.9.24's feature-flagged `/threads/{thread_id}/stream/events` as the deployed Agent Protocol v2 draft dialect and record its difference from upstream `/threads/{thread_id}/stream` in a versioned support matrix, because P0 source inspection proved the event model exists but the originally planned upstream path would return 404 and the route returns 503 without `FF_V2_EVENT_STREAMING=true`. Revisit when Aegra aligns the route with upstream or publishes a stable conformance version. → [docs/plans/rag-restack.md](docs/plans/rag-restack.md)
- 2026-07-26: Keep Aegra-specific AP v2 translation at one typed transport boundary, including `/stream/events`, the `run.start`/`input.respond` command subset, no WebSocket, and HITL `value` to upstream `payload`, because P0 proved Aegra 0.9.24 is a draft dialect rather than wire-identical to the pinned upstream schema. Revisit on every Aegra or Agent Protocol schema bump. → [docs/plans/rag-restack.md](docs/plans/rag-restack.md)
- 2026-07-26: Use one isolated Aegra REST compatibility bridge for cancel, checkpoint state/fork, and history while AP v2 owns streaming plus `run.start`/`input.respond`, because Aegra 0.9.24 returns `unknown_command` for the v2 commands assistant-ui would otherwise need for cancel, Edit/Regenerate, and history. Revisit each fallback when Aegra expands its v2 command dispatcher. → [docs/adr/0005](docs/adr/0005-adopt-assistant-ui.md)
- 2026-07-26: Define the retrieval corpus as Nuartz's published set, excluding basename-leading `_` files and allowing the three currently public no-frontmatter documents only by exact owner-reviewed DocId, because the source has 336 Markdown files while the live site has 335 and the Python agent currently indexes the hidden `_index.md`. Revisit if Nuartz publication semantics or those three legacy files change. → [docs/plans/rag-restack.md](docs/plans/rag-restack.md)
- 2026-07-26: Require `ci/check`, `protocol/compat`, and `wiki/verify` as stable branch gates while keeping component work path-aware behind `ci/check`, because workflow-level path filters leave required checks pending and cannot be conditionally required. Revisit if GitHub adds conditional required workflows or CI cost requires reusable-workflow fan-out. → [docs/plans/rag-restack.md](docs/plans/rag-restack.md)
- 2026-07-28: Give the native Aegra agent separate preview/production Cloud Run services plus same-environment-digest migration and real-Neon grant-probe jobs, with isolated builder/repository/deployer trust boundaries, exact caller/reusable-workflow OIDC claims, separate environment-gated build/deploy runners (and therefore two deliberate production approvals), and bounded registry retention of 14 days/20 preview versions and 90 days/30 production versions, because untrusted preview code must not poison production provenance, writer and deployer credentials must not coexist, and tagged images need an explicit cleanup/rollback contract. Revisit when Aegra supports safe multi-instance serialization, approval friction outweighs credential isolation, rollback retention changes, or deployment moves away from Cloud Run. → [docs/runbooks/cloud-run-delivery.md](docs/runbooks/cloud-run-delivery.md)
- 2026-07-28: Bootstrap agent delivery through explicit `foundation → jobs → services` Terraform stages and bind every Cloud Run secret reference to a reviewed numeric version, because registry/secrets must exist before an image can be pushed, migrations must pass before a service starts, and `latest` can change after scale-to-zero without revision smoke or reliable rollback. Revisit if Cloud Run gains immutable secret aliases or the bootstrap ordering can be proven transactionally. → [docs/runbooks/cloud-run-delivery.md](docs/runbooks/cloud-run-delivery.md)
- 2026-07-26: Regenerate Agent Protocol bindings with the upstream frozen pnpm toolchain and compile every fixture through an isolated TypeScript 5.9.3 consumer, because vendored-byte checks alone cannot prove the CDDL still produces those bindings or that TypeScript consumers accept the fixtures. Revisit on each protocol/toolchain bump or if upstream publishes signed reproducible artifacts. → [protocol/README.md](protocol/README.md)
- 2026-07-26: Preserve corpus-derived Kiwi terms as audit candidates but activate only owner-reviewed seeds with deny precedence, and serve deterministic fitted SQLite instead of raw tokens or runtime BM25 fitting, because automatic NNP promotion polluted morphology and registry construction doubled memory. Revisit if candidate promotion gains an owner-review workflow or another sparse engine replaces Kiwi/rank-bm25. → [docs/reference/retrieval-methods.md](docs/reference/retrieval-methods.md)
- 2026-07-26: Build and evaluate deployable Kiwi/BM25 artifacts in one pinned Linux x86_64 image and scope byte determinism to that target, because Kiwi's optimized kernels produce small architecture-specific floating-point differences despite pinned package and model versions. Revisit if Kiwi guarantees cross-architecture equivalence or the tokenizer artifact becomes architecture-neutral. → [docs/reference/retrieval-methods.md](docs/reference/retrieval-methods.md)
- 2026-07-26: Start the Cloud Run agent at 1 GiB instead of its 512 MiB default, because the clean Linux x86_64 BM25 runtime alone peaks near 373 MiB before Aegra, API, database pools, and concurrent requests load. Revisit after the P2 full-image cold-start and concurrency-20 memory smoke. → [docs/plans/rag-restack.md](docs/plans/rag-restack.md)
- 2026-07-27: Define `main` governance as PR-only with zero mandatory approvals, stable `ci/check`/`protocol/compat`/`wiki/verify` gates, blocked deletion/force-push, and advisory CODEOWNERS, because a solo repository needs enforceable automation without an impossible self-review. Revisit when another maintainer can satisfy required reviews or the stable check contract changes. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-27: Disable Deep Agents' auto-added general-purpose subagent for every configured model until bounded subagent capabilities land, because its `task` dispatch is not yet covered by the shared run budget and approval gates. Revisit after the QuickJS/subagent capability experiment enforces those gates across nested dispatches.
- 2026-07-27: Run web CI on Node 24.14.0 for ESLint 10 and wrap Next's flat configs with the official `@eslint/compat` bridge, because `eslint-config-next` still bundles rules that call RuleContext methods removed in ESLint 10. Revisit when every bundled Next plugin supports ESLint 10 natively.
- 2026-07-27: Select the chat's ranked retriever with server-owned `RAG_RETRIEVER_METHOD` (default `bm25`) and resolve it only through the servable registry against the verified `agent/.index` snapshot, because the manual chat and evaluation harness must exercise the same implementation without accepting a visitor-controlled method override. Revisit when deployment needs per-request A/B assignment with an equally trusted selector. → [docs/reference/retrieval-methods.md](docs/reference/retrieval-methods.md)
- 2026-07-27: Keep routine Preview deployments free of mandatory review while retaining the existing owner review on Production with self-review allowed, because every contributor needs a usable preview and a solo-owner production gate must not deadlock. Revisit when another maintainer can provide independent production approval or preview spend requires gating. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-27: Bind every required check context to GitHub Actions integration ID 15368 and allow exactly the four documented main ruleset types, because a matching name from another app or an unnoticed extra deployment rule is not the reviewed gate. Revisit if a required check moves to another GitHub App or the main ruleset intentionally gains a rule. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-27: Bind each required check to one top-level workflow/job and its exact PR, main-push, merge-queue, and manual triggers, and keep `main` rulesets-only with legacy branch protection absent, because an ignored nested workflow, filtered trigger, skippable dependency, or overlapping protection surface can leave the reviewed gate missing or ambiguous. Revisit if GitHub recognizes nested workflows, adds conditional required workflows, or branch protection is intentionally restored. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-27: Compare the main ruleset's repository source, default-branch-only target, enforcement, and complete PR/status parameter objects exactly, with all three repository-enabled merge methods and every solo-owner review gate deliberately disabled, because omitted defaults and a broad `~ALL` condition can silently change mergeability while headline approval/check counts still match. Revisit when another maintainer enables independent review or the GitHub ruleset schema adds a reviewed parameter. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-27: Keep a hardcoded reviewed governance baseline alongside the editable manifest and update both plus mutation tests in one PR, because comparing GitHub only to a freely edited manifest lets repository identity, required checks, Actions, environments, or Dependabot drift by changing the expected value itself. Revisit if the baseline is generated from a separately approved signed policy. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-27: Require vulnerability alerts plus enabled, unpaused Dependabot security updates as external repository state, cross-checking the dedicated endpoint against the admin-only repository security summary, because `dependabot.yml` schedules version updates but does not enable either security feature and one API surface alone can be incomplete. Revisit if GitHub consolidates these settings behind one authoritative response. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-27: Treat GitHub's omission of a disabled optional `dismissal_restriction` as the exact repository-ruleset response contract and stage new rulesets as disabled before activation, because API 2026-03-10 normalizes that submitted object away and an exact preflight prevented activating an unverifiable gate. Revisit if GitHub begins returning the disabled object or changes the ruleset response schema. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-27: Store the GCP foundation state under a dedicated versioned, public-access-blocked GCS backend with native locking and retain a restricted local pre-migration backup, because the prior local file was the only authoritative copy and could not support safe CI or concurrent operators. Revisit if state moves to a managed Terraform service. → [infra/gcp](infra/gcp)
- 2026-07-27: Keep Auth.js v5 with `@auth/pg-adapter` and use Neon only as Postgres, because the existing OAuth and session architecture already works and adopting Neon Auth would add an unrelated migration. Revisit if Auth.js is no longer maintained or Neon Auth supplies a required capability. → [docs/runbooks/gcp-neon-foundation.md](docs/runbooks/gcp-neon-foundation.md)
- 2026-07-27: Isolate preview and production runtime identities, secrets, and deployer impersonation while withholding project-wide Cloud Run admin and image-writer access, because no Cloud Run service exists yet and build and deploy identities have different duties. Revisit when service-scoped CD and a separate image builder are introduced. → [infra/gcp](infra/gcp)
- 2026-07-27: Retain the existing shared `github` Workload Identity pool with exactly the enabled `github-preview` and `github-production` providers, because replacing live federation resources without a state/import and credential-transition plan risks a deployment outage. Revisit when the CD workflows exist and a reviewed split-pool migration can preserve both environments throughout the transition. → [infra/gcp](infra/gcp)
- 2026-07-27: Parse the foundation with ephemeral pinned `python-hcl2==7.3.1` and verify its exact structural inventory instead of matching Terraform source with regular expressions, because valid HCL comments and line breaks can hide security-relevant blocks from text patterns. Revisit if Terraform exposes a stable credential-free configuration JSON command or this parser stops tracking Terraform HCL. → [scripts/ops_foundation_contract.py](scripts/ops_foundation_contract.py)
- 2026-07-27: Exact-compare every parsed GCP foundation resource, local, check, output, variable, provider, data, backend, and import body; SHA-256 pin the sole Terraform test file and require its one named JSON-reported run, because partial body checks plus grep comments and Terraform's successful zero-test exit left destructive drift unguarded. Revisit if Terraform exposes a stable machine-readable configuration and test-discovery contract that covers valid test expressions directly. → [scripts/ops_foundation_contract.py](scripts/ops_foundation_contract.py)
- 2026-07-27: Enumerate every on-disk Terraform configuration, override, automatic variable, and test candidate before invoking Terraform and require the exact regular tracked allowlist, because gitignored override files are loaded by Terraform but invisible to a `git ls-files` inventory. Revisit if Terraform exposes a non-executing authoritative file-discovery command. → [scripts/ops_foundation_contract.py](scripts/ops_foundation_contract.py)
- 2026-07-27: Mirror Terraform 1.13.5's native `.tf`, `.tfvars`, `.tftest.hcl`, and `.tfmock.hcl` format candidates plus reviewed JSON/load variants, and permit `.terraform` only as an ignored, untracked real directory whose contents stay opaque, because recursive formatting reads generic variable and mock files while local initialization needs an internal cache that must never become a tracked or redirected input. Revisit on the next Terraform version bump. → [scripts/ops_foundation_contract.py](scripts/ops_foundation_contract.py)
- 2026-07-27: Configure Dependabot with the repository's native `bun` and `uv` ecosystems rather than `npm` and `pip`, because native support updates `bun.lock` and `uv.lock` with their manifests and prevents predictably red dependency PRs. Revisit if either lockfile or package manager changes. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-27: Keep React/React DOM and their type packages plus NumPy out of routine Dependabot groups, because React updates require UI evidence and NumPy is embedded in the persisted BM25 provenance contract. Revisit if UI compatibility tests and artifact regeneration become automatic. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-27: Keep an npm security-only `/web` Dependabot identity beside native Bun version updates, because GitHub updates `bun.lock` for Bun version PRs but does not yet support Bun security-update PRs. Revisit when GitHub adds Bun security updates; until then, regenerate `bun.lock` manually in every npm security PR. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-27: Make `ci/agent` reject a stale `uv.lock` before its frozen install and require `--frozen` on every project-bound `uv run`, because a later non-frozen run can silently re-resolve dependencies and replace the environment CI claims to verify. Revisit if uv provides an enforceable job-wide immutable-resolution mode. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-27: Bind the complete `ci/agent` job and ordered action/run steps to one reviewed AST baseline, because a frozen command can still be bypassed by changing its runner, container, checkout target, setup action, environment, or execution order. Revisit when an intentional CI change updates the baseline and its mutation suite together. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-27: Remove the unused direct Chroma dependency while keeping the method explicitly rejected in the retrieval registry, because it has zero call sites and its critical advisory has no patched release; a vector-store choice should follow the dense-embedding experiment instead of preceding it. Revisit if an evaluated dense method selects a maintained Chroma integration. → [docs/reference/retrieval-methods.md](docs/reference/retrieval-methods.md)
- 2026-07-27: Disable user-facing thread deletion on Aegra 0.9.24, because its native route deletes metadata but strands checkpoints and it exposes no supported operation that commits both atomically; a future administrative orphan-GC/retention job is separate and is not a substitute for user deletion. Revisit when Aegra provides an atomic metadata-plus-checkpoint deletion primitive. → [docs/adr/0004](docs/adr/0004-adopt-aegra.md)
- 2026-07-27: Expose only AP v2 `run.start`/`input.respond` for owner-preview model and checkpoint mutations and hide legacy run, state-update, and cron-spend routes with a non-enumerating 404, because those native Aegra paths bypass the single-process guard; keep read/history/cancel compatibility. Revisit when Aegra serializes every mutation or the compatibility surface gets an equivalent guard. → [docs/adr/0004](docs/adr/0004-adopt-aegra.md)
- 2026-07-27: Run the Aegra migration, checkpoint, real `/memories/` isolation, and pool-recreation suite against PostgreSQL 17 in every agent CI run, because an opt-in local test that silently skips cannot protect the persistence contract. Revisit if the integration moves to a dedicated required workflow with the same fail-closed coverage. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-27: Roll setup-python to immutable v7.0.0 and setup-uv to immutable v8.3.2 together across every workflow, and explicitly checksum dependency-audit's uv 0.11.29 download, because both actions move their runtime/dependency wave while setup-uv v8.3.2's built-in checksums end at 0.11.28. Revisit if the dependency-audit dispatch cannot install uv 0.11.29 or either action changes a used input. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-27: Keep production web dependencies at zero high/critical findings while permitting only CVE-2026-14257 in the exact dev-only `eslint-config-next` lint chain through 2026-08-31, because upstream has no CommonJS-compatible patched Brace Expansion release and forcing version 5 breaks Minimatch 3. Revisit immediately on any advisory/path drift or compatible upstream fix. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-28: Keep migration and runtime Neon credentials separate, but temporarily add to the runtime's narrow DML grants only the schema-local idempotent DDL required by Aegra 0.9.24's unconditional LangGraph saver/store setup, because disabling startup Alembic migration does not provide a no-DDL lifespan. Block deployment until the exact grants and denial boundaries pass against real Neon, and tighten to DML-only when Aegra exposes a supported no-DDL startup. → [docs/runbooks/gcp-neon-foundation.md](docs/runbooks/gcp-neon-foundation.md)
- 2026-07-28: Emit exact bounded retrieval-only `syshin.rag.inspection.v1` observations as best-effort live-run data through a compiled LangGraph custom-stream transformer and Aegra's native Agent Protocol channel, because the evaluation UI needs measured single-stage method/corpus/source provenance without parsing tool prose, durably retaining visitor queries, or exposing prompts, identities, raw content, and future capability guesses. Revisit when Agent Protocol standardizes these fields or measured component stages, QuickJS, or subagents require v2. → [docs/reference/rag-inspection-events.md](docs/reference/rag-inspection-events.md)
- 2026-07-27: Make canonical JSON under gitignored `eval/results/<content-tree-sha>/<run-id>/run.json` the retrieval-evaluation system of record and derive Markdown/SVG from it, because query rankings and fingerprints must remain reproducible after hosted traces expire without committing generated sweeps. Revisit if result volume needs an immutable artifact store. → [eval/README.md](eval/README.md)
- 2026-07-28: Resolve `agent` and `eval` as one root uv workspace with one lock while syncing and exporting members by package, because independent locks produced conflicting shared versions but evaluation-only tooling must remain outside the deployed agent install. Revisit if uv can prove cross-project resolution consistency without a workspace lock. → [eval/README.md](eval/README.md)
- 2026-07-28: Bind evaluation run IDs to agent/eval source trees, the workspace lock, platform, Python, and execution-image provenance, and publish only digest-pinned Linux x86_64 runs, because method fingerprints alone allowed different code to reuse an identity and the prior macOS ARM bootstrap was not deployment-comparable. Revisit if signed build attestations replace environment-recorded image provenance. → [eval/README.md](eval/README.md)
- 2026-07-28: Replace caller-recorded evaluation image provenance with a main-only, environment-gated GitHub workflow that executes a pinned Linux amd64 image by its derived digest and attests an exact result-manifest archive, because a process or environment variable cannot prove which image launched it. Promotion additionally requires owner-reviewed qrels and external attestation verification. Revisit if the runtime can consume a verifiable workload identity directly. → [eval/README.md](eval/README.md)
- 2026-07-28: Gate evaluation attestation with a dedicated `Evaluation Publication` GitHub environment rather than `Production`, because the Production environment participates in GCP deployment identity and must not grant that trust context to an evaluation workflow. Keep the evaluation environment main-only, reviewer-gated, admin-non-bypassable, secretless, and outside every GCP WIF condition. Revisit if publication and deployment move behind one equally narrow reusable trusted builder. → [docs/runbooks/gcp-neon-foundation.md](docs/runbooks/gcp-neon-foundation.md)
- 2026-07-28: Give evaluation publication its own Dockerfile-specific context allowlist while the root `.dockerignore` remains a shared secret and build-artifact denylist, because Cloud Run image inputs must be able to evolve without broadening what reaches the evaluation builder. Revisit if the two images build from separate context roots. → [eval/Dockerfile.publication.dockerignore](eval/Dockerfile.publication.dockerignore)
- 2026-07-28: Verify every evaluation method against the reviewed registry plus verified corpus artifacts and exact qrel-by-qrel ranking replay, and require publication verification from the exact clean attested commit with matching agent/eval source-tree and lock digests, because internally consistent result resealing otherwise permits unregistered identities, altered rankings, or a different checkout to cross the publication boundary. Revisit if a signed registry, ranking, and source manifest replaces local reconstruction. → [eval/README.md](eval/README.md)
- 2026-07-28: Supersede the 2026-07-27 two-enabled-provider entry by making `github-production` the sole active agent-delivery WIF provider with explicitly mapped numeric repository/owner IDs and four exact mapped `delivery_role` principals, while retaining `github-preview` disabled, inert, Terraform-managed, and protected from destruction, because provider conditions must use mapped fields and phase-specific trust can share one audited issuer without leaving a second usable provider or forcing destructive retirement in the hardening apply. Revisit after the disabled provider has remained unreferenced across two verified production releases and a separate destroy plan is approved. → [docs/runbooks/cloud-run-delivery.md](docs/runbooks/cloud-run-delivery.md)
- 2026-07-28: Supersede the 2026-07-27 setup-uv v8 baseline by pinning setup-uv v9.0.0, uv 0.11.29, and its reviewed checksum across every workflow, requiring exactly uv 0.11.29 at the workspace root, and binding the agent image to the same version and immutable digest, because a frozen dependency lock does not make the resolver/installer executable reproducible. Revisit only as one coordinated toolchain update with workflow, Docker, governance, and mutation-test pins. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-28: Supersede the same-day two-approval delivery entry and narrow the 2026-07-27 review-free-preview entry to Vercel Preview: run the isolated agent image builder without a GitHub environment or secrets, then require one owner-reviewed, admin-non-bypassable release job for both agent preview and production, because one environment cannot guarantee two independent approvals and unreviewed pull-request code must not receive the smoke token, migration access, or traffic authority. Revisit if GitHub adds independently consumable multi-stage approvals or preview releases no longer carry spend or runtime credentials. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-28: Supersede the ephemeral-parser part of the 2026-07-27 foundation-verifier entry by pinning `python-hcl2==7.3.1` in the root frozen uv workspace used by local verification and CI, because dynamic tool resolution makes the structural Terraform contract depend on package-index state. Revisit if the verifier moves to a signed standalone tool or Terraform exposes a stable credential-free configuration JSON command. → [scripts/ops_foundation_contract.py](scripts/ops_foundation_contract.py)
- 2026-07-28: Replace the predefined Cloud Run developer role with the exact seven-permission `cloudRunAgentDelivery` custom role and bind each verified Job `etag` to REST `jobs.run` plus its immutable successful Execution, because deletion, override, or verification/execution drift must not fit inside the deployer boundary. Revisit if Cloud Run exposes a narrower first-party deployment role with the same optimistic-concurrency contract. → [docs/runbooks/cloud-run-delivery.md](docs/runbooks/cloud-run-delivery.md)
- 2026-07-28: Revalidate the exact release tuple after approval and before GCP auth, require current-main successful checks only for deployment, keep manual exact-revision rollback usable on red CI, and run the full authenticated APv2 smoke on the tagged no-traffic revision before promotion, because approval queues, branch movement, failed CI, and protocol drift must not expose an unverified revision or disable emergency recovery. Revisit if GitHub provides immutable approval-bound artifacts and Cloud Run offers an atomic pre-traffic protocol gate. → [docs/runbooks/cloud-run-delivery.md](docs/runbooks/cloud-run-delivery.md)
- 2026-07-28: Fix service runtime `REDIS_BROKER_ENABLED=false` and `BG_JOB_MAX_RETRIES=0`, and require every approved release to use a freshly rotated owner smoke JWT with at most two hours total lifetime and at least 65 minutes remaining, because bounded background execution must fail closed while the static GitHub environment secret cannot autonomously mint the `exp`-required token. Revisit when a narrowly scoped per-release mint replaces manual rotation. → [docs/runbooks/cloud-run-delivery.md](docs/runbooks/cloud-run-delivery.md)
- 2026-07-28: Supersede the 2026-07-26 concurrency-20 Cloud Run sizing target with exact 1 GiB, `cpu_idle=true`, `startup_cpu_boost=true`, 300-second timeout, `max_instances=1`, concurrency 8, and one worker, and make `ci/agent` migrate and boot the exact built Linux amd64 image against PostgreSQL 17 before checking health and unauthenticated APv2 rejection, because the reviewed runtime must preserve the process-local guard and CI must prove deployable packaging without making a provider request. Real Neon grants, provider-backed conversation, browser, and capability evidence remain P2/P3 operational gates. Revisit after Aegra provides distributed serialization or production measurements justify a coordinated runtime-contract change. → [docs/plans/rag-restack.md](docs/plans/rag-restack.md)
- 2026-07-28: Supersede the 2026-07-26 assistant-ui patch pins with `@assistant-ui/react` 0.14.28 and `@assistant-ui/react-langgraph` 0.14.13 while retaining the native LangGraph adapter boundary, because P3 should begin from the current reviewed npm patch releases rather than immediately implement known-stale pins. Revisit as one coordinated UI dependency, lockfile, fixture, and browser-evidence update. → [docs/plans/rag-restack.md](docs/plans/rag-restack.md)
- 2026-07-28: Remove OpenAI credentials from the repository-owned preview and production Cloud Run delivery contract and forget any legacy Secret Manager objects from Terraform state without destroying them, because the deployed model is fixed to Anthropic and an unused provider credential expands the secret surface. Revisit only if a reviewed provider experiment restores OpenAI through an isolated delivery contract. → [docs/runbooks/cloud-run-delivery.md](docs/runbooks/cloud-run-delivery.md)
- 2026-07-28: Create one non-serializable atomic `RunBudget` in Aegra's per-run async graph factory and share it through root and declarative-child middleware, authorizing `task` only from `ServerRuntime.user.permissions` while Redis and background retries remain disabled, because config/state/checkpoint budgets can be forged, reset by children, or replayed after persistence. Revisit when multi-instance execution has one transactional distributed ledger with retry identity. → [docs/plans/rag-restack.md](docs/plans/rag-restack.md)
- 2026-07-28: Supersede the declarative-child portion of the preceding run-budget decision with public Deep Agents `CompiledSubAgent` boundaries that pass and return messages only, mount exactly the blog-retrieval skill through an isolated backend, and permit production generation only after Anthropic's official tool-aware input count succeeds; temporarily reject OpenAI chat models because LangChain's local counter omits tool schemas and unproven byte estimates cannot enforce a pre-spend cap. Revisit OpenAI only after its Responses input-count payload is proven identical to generation, and revisit shared child backends only if Deep Agents adds a public per-child state/backend allowlist. → [docs/plans/rag-restack.md](docs/plans/rag-restack.md)
- 2026-07-28: Require capability evaluation to consume an immutable terminal `RunBudget` snapshot with visible open reservations and middleware-derived Anthropic pricing buckets, because executor-reported token observations and nonterminal snapshots cannot prove exact settlement. Missing provider details remain explicitly incomplete, and this run-local evidence does not replace P5's identity/global spend gates. Revisit when the provider usage schema or ledger boundary changes. → [docs/plans/rag-restack.md](docs/plans/rag-restack.md)
- 2026-07-28: Audit exact Aegra, Agent Protocol, Deep Agents, LangGraph, LangGraph SDK, native QuickJS, and activated assistant-ui pins weekly against stable official PyPI, npm, and GitHub releases with one read-only fail-closed script, because isolated compatibility dependencies need a visible human-review trigger without an automatic update crossing protocol or runtime boundaries. Revisit if a signed upstream feed or dependency bot can enforce the same exact pin, stable-release, canonical-source, and no-auto-update contract. → [docs/runbooks/upstream-version-audit.md](docs/runbooks/upstream-version-audit.md)
- 2026-07-28: Keep native QuickJS middleware topology-stable but deployment-opted-out by default, advertise and execute its coroutine-only call-mode tool solely for `admin`/`eval`, expose no PTC/task/console/host bridge, and atomically reserve its tool, concurrency, and UTF-8 output limits in the run ledger, because Aegra access contexts need one topology while client config, sync execution, raw runtime errors, cancellation, and checkpoint replay must not expand the sandbox or reset spend. Revisit only after the P4.5 quality gate and deployed P5/P6 abuse tests justify another tier or persistence mode. → [docs/reference/quickjs-capability.md](docs/reference/quickjs-capability.md)
- 2026-07-28: Select the dynamic-subagent axis for the 2×2 capability experiment only through an exact-boolean server-owned `create_graph` argument, with `True` still intersected with `admin`/`eval` authorization and no client-config or environment selector, because QuickJS-only evidence requires independent axes without creating a privilege-escalation input. Revisit when the experiment is retired or a signed evaluation manifest owns capability selection. → [docs/reference/quickjs-capability.md](docs/reference/quickjs-capability.md)
- 2026-07-28: Treat Turnstile success as the anonymous identity-creation gate, then use a dedicated Vercel-only `ANONYMOUS_SESSION_SECRET` to seal one absolute, non-sliding 14-day remint cookie, because guest continuity must not reuse or expand the blast radius and rotation contract of the shared owner JWT key. Invalid or expired cookies require a new challenge and identity. Revisit when server-side session revocation or shorter retention replaces the sealed-cookie credential. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-07-28: Build WEB-A with assistant-ui's native `useLangGraphRuntime` over the official AP v2 `ThreadStream`, a root-only content pump, and a physically separate lifecycle/input watcher, while prohibiting anonymous WEB-B until state/history and wildcard input have server-side safe projections and reasoning is suppressed/redacted before the wire or separately proven absent, because UI reduction cannot undo open graph state, future sensitive interrupt payloads, or root-message reasoning that already crossed the browser network boundary. Revisit when Aegra/the SDK can enforce those public-safe filters or the graph exposes separately proven bounded schemas and a no-reasoning-wire model path. → [docs/adr/0005](docs/adr/0005-adopt-assistant-ui.md)
- 2026-07-28: Keep public agent execution behind its own exact opt-in and accept canonical `anon:<uuid4>` tokens only through a pure-ASGI Agent Protocol route/body/stream/rate boundary, an explicit lower-output model and run budget, and no cross-thread persistent memory, because Turnstile identity minting alone does not constrain capabilities or provider spend. Keep the opt-in off until the durable daily ledger and expired-thread GC ship; revisit the process-local limiter only with transactional multi-instance serialization. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-07-28: Before every paid guest command, atomically reserve a reviewed worst-case micro-dollar amount against one PostgreSQL UTC-day ceiling, and reap only exact retention-marked canonical guest threads by deleting LangGraph checkpoints before Aegra thread parents under a bounded advisory-locked sweep, because process memory cannot enforce a shared spend ceiling and parent-only deletion does not reclaim the storage-heavy state. Reservations are intentionally not refunded after dispatch; revisit when provider-signed usage can settle the same durable reservation without reopening a pre-spend race. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-07-28: Run anonymous maintenance as a same-image Cloud Run Job after every release and every 15 minutes through a production-only Cloud Scheduler identity that can invoke only that exact job, because retention cleanup and bounded stale-run repair must survive idle traffic without giving the scheduler database credentials or broad Cloud Run authority. Revisit if maintenance moves to a database-native policy or one externally managed maintenance plane. → [docs/runbooks/cloud-run-delivery.md](docs/runbooks/cloud-run-delivery.md)
- 2026-07-28: Rebuild every allowlisted guest thread/run/command/state/history JSON response and complete AP v2 SSE frame through one bounded server-side public-wire projection, force both SDK streams to the root namespace at depth zero, and remove raw entities, errors, reasoning, and tool/input payloads before response body bytes leave the process, because browser-side reduction cannot retract data that already crossed the network. Revisit when Aegra provides an equivalent versioned public schema. → [docs/adr/0005](docs/adr/0005-adopt-assistant-ui.md)
- 2026-07-28: Run the native same-thread acceptance guard outside the guest spend guard, validate both paid guest wires before claiming capacity, and serialize each valid guest command with retention GC on one deterministic PostgreSQL per-thread transaction advisory key held from pre-ownership validation through scheduling or parent-delete commit, because rejected commands must not spend and GC must neither delete under an accepted command nor expose an uncommitted old parent to a waiting command. This is not full scheduling atomicity: revisit with a durable expiring command claim or transactional queue before multi-instance rollout, or if a lost PostgreSQL backend can let downstream scheduling outlive its advisory lock. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-07-28: Stage WEB-B behind a separate exact public UI opt-in as an outer Turnstile/cookie-resume gate that validates one canonical anonymous credential and injects it into the existing native assistant-ui token broker, because anonymous identity discovery must not create a second chat transport or persist the one-use challenge token. Keep the UI opt-in off until deployed raw state/history/SSE proofs satisfy ADR-0005. Revisit when the agent token route and browser runtime share a server-issued opaque bootstrap envelope instead of client-readable JWT claims. → [docs/runbooks/public-anonymous-chat.md](docs/runbooks/public-anonymous-chat.md)
- 2026-07-28: Advance the native chat boundary as one reviewed unit to `@assistant-ui/react` 0.15.0 and `@assistant-ui/react-langgraph` 0.14.15 while retaining the exact Agent Protocol 0.0.18 and LangGraph SDK 1.9.28 pins, because the official registry audit found only the assistant-ui pair stale and their compatibility must be proven together by unit, APv2, browser, responsive, and IME evidence. Revisit on the next stable registry release or an incompatible native runtime contract. → [docs/adr/0005](docs/adr/0005-adopt-assistant-ui.md)
- 2026-07-28: In the Redis-off Aegra 0.9.24 deployment, fence each canonical guest execution on a dedicated unpooled PostgreSQL physical connection with a deterministic session advisory lock held, while that database session survives, until committed terminal state or the owning Aegra task ends; let 15-minute bounded recovery acquire the same key transactionally before failing only exact retention-marked, lease-free orphans. Elapsed time cannot distinguish a crash from live execution, while using Aegra's two-connection ORM pool for fences can deadlock finalization; the next entry covers lost fence sessions. Revisit when Aegra provides Redis-off crash recovery or the deployment enables its worker leases. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-07-28: Harden Redis-off guest recovery with cancellation-shielded PostgreSQL polling, owner cancel-and-drain before voluntary fence release, and an append-only project migration whose `BEFORE UPDATE` trigger rejects every later write to a run atomically marked as recovered, because backend termination destroys a session advisory lock before the live Aegra finalizer is drained and `finalize_run` otherwise permits error-to-success resurrection. New runs on the same thread and deletion remain valid; revisit when Aegra exposes a native compare-and-set finalizer or durable local-executor epoch. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-07-28: Start each Redis-off guest owner monitor immediately after fence acquisition and before graph compilation or yield, then defer expired-thread GC for 15 minutes from the recovered run's immutable marker timestamp, because a killed PostgreSQL backend releases its lock before monitor detection and a same-sweep delete could otherwise orphan or race a checkpoint written in that gap. The monitor spans graph execution and Aegra finalization, cancels and drains the owner on session loss, and later sweeps retain checkpoint-first deletion. Revisit when recovery and execution share one durable transactional owner epoch. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-07-29: Supersede the preceding age-based GC grace with a durable `(run_id, thread_id, identity)` quarantine, a four-slot PostgreSQL session-advisory execution cap, and success-counted recovery/GC batches of at most 1,000 over independently bounded 2,000-candidate materializations: stale recovery records `recovered_at = clock_timestamp()` in its status transaction, while guest `run.start` and both GC candidate reads remain blocked until the monitor has awaited the terminal owner plus its pending database operation and committed `drained_at` through a new bounded connection before voluntary fence release. A proof written before recovery is preserved; a hard crash that writes no proof never auto-releases by age, and locked head candidates do not consume the requested success count. Revisit when Aegra provides a durable executor epoch whose terminal transition and checkpoint writers share one transaction. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-07-29: Supersede automatic activation of the production guest-maintenance schedule by creating it paused through a repository-owned Terraform constant and requiring a separate reviewed public-launch, billing approval, and exact plan to unpause it, because delivery enablement is not a spend kill switch and unattended recurrence must fail closed without implying zero cost. Revisit if maintenance moves to a database-native or separately audited scheduling plane. → [docs/runbooks/cloud-run-delivery.md](docs/runbooks/cloud-run-delivery.md)
- 2026-07-29: Disable Vercel Git Integration deployments for `main` in the official static project config while leaving every unlisted contributor branch eligible for Preview, because a production push must not move traffic before a separately reviewed delivery gate can prove and promote its exact artifact. Revisit if Vercel removes branch-level `deploymentEnabled` or Git Integration is retired. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-29: Keep GCP foundation live verification non-operational: unsigned v1 validates single-organization structure only, no-argument execution defaults to `--static`, and explicit `--live` validates that structure then stops at the missing signed company-admin-attestation gate with no GCP CLI helper or request implementation, because self-consistent JSON cannot authenticate company-admin origin, complete inherited IAM, or project-parent linkage and dormant cloud execution code is an unnecessary reactivation risk. Support the verifier only through direct `/bin/bash -p` execution; add live reads later only as a separate reviewed signed-v2 change with a repository-pinned company-admin public key. → [docs/runbooks/gcp-neon-foundation.md](docs/runbooks/gcp-neon-foundation.md)
- 2026-07-28: Keep Auth.js v5 rather than adopting beta Neon Auth, bind its official PostgreSQL adapter to a committed idempotent UUID-text schema, run Vercel on the project-pinned Node 24.x runtime, create and explicitly close one bounded `@neondatabase/serverless` Pool inside each lazy auth operation, and reserve `pg` for transaction-owning migration and PostgreSQL integration checks, because provider linking, connection lifecycle, rollback, and the agent's canonical string subject boundary must remain explicit and testable without exposing DDL authority to Vercel. This establishes the application contract only; production promotion remains blocked on a separately approved preview/production migration gate and credential. Revisit when Neon Auth is stable and can preserve the custom subject/scope contract, or Auth.js, Node, or Neon deployment guidance changes. → [docs/runbooks/web-auth.md](docs/runbooks/web-auth.md)
- 2026-07-29: Parse each Auth.js database URL exactly once into a typed direct-Neon pool contract with fixed search path and timeouts, accept only `sslmode=require` outside an explicit loopback test CLI path, and reject non-empty `PG*` fallback, because raw URL reparsing or ambient driver settings can change the database target after validation. Revisit only with an equally single-source typed driver boundary. → [docs/runbooks/web-auth.md](docs/runbooks/web-auth.md)
- 2026-07-29: Require the Auth.js schema verifier to reject every deviation from the exact 24-column, seven-index, nine-constraint, eight-RI-trigger PostgreSQL 17 surface, including orphan rows, inheritance, legacy tables, RLS, rules, and write hooks, because a nominal table check cannot prove adapter writes retain their intended semantics. Revisit only with an intentional reviewed adapter-schema revision. → [docs/runbooks/web-auth.md](docs/runbooks/web-auth.md)
- 2026-07-29: Supersede the same-day Vercel `main` opt-out and restore the Git Integration default for both `main` Production and contributor Preview deployments by removing the checked-in top-level `git` object, because this personal blog already relies on automatic Vercel delivery and the company GCP cost/IAM boundary does not justify disabling that separate delivery path; automatic deployment is not an Auth/Neon migration gate, so the restoration itself and revisions that depend on a schema or environment change must prove those prerequisites before merge. Revisit if the Vercel project is unlinked or a user-approved replacement delivery path is proven end to end. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-29: Amend the Auth.js verifier's PostgreSQL 17 catalog wording to require the same 24-column, seven-index, nine non-`NOT NULL`-constraint, eight-RI-trigger contract on PostgreSQL 17 and 18, plus a complete validated, enforced, local, non-inherited PostgreSQL 18 `contype = 'n'` inventory for every expected non-null column when that surface exists, because PostgreSQL 18 moved relation `NOT NULL` specifications into `pg_constraint` and blindly ignoring those rows would hide duplicate or weakened metadata. Generated `NOT NULL` constraint names remain outside the contract so legacy table renames stay compatible. Revisit if names become application-visible or PostgreSQL changes this catalog representation again. → [docs/runbooks/web-auth.md](docs/runbooks/web-auth.md)
- 2026-07-29: Require every Auth.js primary-key, unique, and foreign-key constraint to remain enforced and non-period on PostgreSQL 18, because matching columns and names alone would otherwise accept `NOT ENFORCED`, `WITHOUT OVERLAPS`, or `PERIOD` semantics that the adapter schema never declared. Revisit only with an intentional temporal-key or unenforced-integrity schema revision. → [docs/runbooks/web-auth.md](docs/runbooks/web-auth.md)
- 2026-07-30: Keep the Cloud Run anonymous runtime contract repository-owned at an exact disabled flag plus blank guest model and spend ceilings until a separate reviewed paid public-launch PR splits Preview from Production and sets the Production four together, because apply-time variables, shared activation, or partial console values could bypass the intended environment/cost gate or make the agent fail at startup. Revisit only when an equally reviewable external configuration system can enforce atomic environment, model, budget, and enablement changes. → [docs/runbooks/public-anonymous-chat.md](docs/runbooks/public-anonymous-chat.md)
- 2026-07-30: Store the provider-free P4.5 QuickJS × dynamic-subagent harness as an exact server-selected four-arm experiment under `eval/results/capabilities/`, with every artifact permanently labelled `synthetic-provider-free` and excluded from retrieval and public-enablement evidence, because fake-model topology proof is useful but cannot support a provider quality or cost claim. Revisit only through a separately reviewed adapter that derives its provider identity and introduces a distinct evidence tier. → [eval/README.md](eval/README.md)
- 2026-07-30: Accept a synthetic P4.5 capability attempt only after the entire executor finishes inside its shared deadline and `RunBudget.finalize()` yields complete normalized Anthropic-shaped token buckets, using isolated immutable-by-contract task copies, a measured matching content tree, fresh execution/attempt/thread/run identities, a deterministic counterbalanced arm schedule, retries only before any spend, and a conservative experiment-wide cost ceiling, because cross-arm mutation, reused persistence, fixed order, omitted paid retries, or self-inconsistent accounting would invalidate even provider-free harness evidence. Revisit when the paid adapter can provide stronger provider-native provenance. → [eval/README.md](eval/README.md)
- 2026-07-30: Keep the owner/evaluation model on Anthropic but lock the future anonymous tier to OpenAI Responses `gpt-5.4-nano`, with reasoning and provider storage disabled, a model-call/output reservation committed before every official input-token count, and exact token-bearing payloads derived by replaying the same public `ChatOpenAI` binding through a no-network capture transport, because local tokenizers omit final tool schemas and a visitor must never select, mutate, or issue an unreserved provider request. Pin and isolate `langchain-openai==1.3.5` plus `openai==2.50.0`, auditing the former below an exclusive 1.4.0 compatibility ceiling while `langchain-core` remains 1.4.9. Keep launch disabled and its model/budget values blank because this model has no API Free tier and the count endpoint's billing semantics are not explicit; the 6,701 µUSD reservation floor covers generation only. Revisit when billing semantics, the core/serialization contract, or a cheaper evaluated model changes. → [docs/runbooks/public-anonymous-chat.md](docs/runbooks/public-anonymous-chat.md)
- 2026-07-30: Keep `syshin0116.vercel.app` as the verified canonical Vercel Production project domain, auto-assign it on every successful `main` Production deployment, and compare its runtime SHA plus unique deployment URL to GitHub's exact post-deploy identity without a Vercel token, because repository Git eligibility alone cannot detect a staged or stale public alias. Revisit if Vercel delivery is replaced or an equally credential-free promotion check owns this boundary. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-31: Roll routine Dependabot version updates into one monthly PR per Bun, uv, and GitHub Actions ecosystem while keeping compatibility-sensitive Aegra/Agent Protocol, LangGraph, deepagents, QuickJS, OpenAI, assistant-ui, Auth, Next, and React stacks in focused PRs (with React, React DOM, and their types coupled) and leaving vulnerability alerts and security updates enabled, because weekly routine updates caused unnecessary preview/CI churn while broad compatibility rollups would violate the repository's version-bump gates. Revisit if monthly routine rollups become too large to diagnose or a focused stack needs a different cadence.
- 2026-07-31: Supersede the anonymous-model portion of the 2026-07-30 entry by locking the still-disabled future guest tier to OpenAI Responses `gpt-5.6-luna` at `reasoning.effort=none` and `reasoning.context=current_turn`, retaining the exact serialization pins and fail-closed count-before-generation contract, sending only a stable 64-character domain-separated SHA-256-derived safety identifier from the random guest subject, and setting the 12,000-token/four-call generation-only reservation floor to 6,892 µUSD at $0.20/M uncached input, $0.25/M cache-write input, and $1.20/M output, because Luna is the GPT-5.6 nano-role model, its default reasoning context and cache-write billing differ from earlier models, and it has no API Free tier, so migration cannot weaken privacy, statelessness, exact usage settlement, or the paid-launch gate. Cached input at $0.02/M never lowers the worst-case pre-dispatch reservation. Revisit when provider pricing, count-endpoint billing, safety-identifier guidance, or evaluated guest quality changes. → [docs/runbooks/public-anonymous-chat.md](docs/runbooks/public-anonymous-chat.md)
- 2026-07-31: Isolate `@neondatabase/*`, TypeScript, and `lucide-react` into independent Dependabot version PRs and raise Bun's open version-PR capacity from seven to 10, because the broad web rollup proved each can independently break the Auth.js pool type boundary, ESLint/Bun/Next toolchain, or brand-icon imports and a seven-slot limit would starve the routine rollup after adding their queues. Revisit if upstream compatibility makes any of the three safe to co-roll or 10 concurrent version PRs create persistent review noise. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-07-31: Supersede the generic Auth.js PostgreSQL-adapter boundary with exact `@auth/neon-adapter` 1.11.3 and `@neondatabase/serverless` 1.1.0 runtime pins, strictly normalize the official adapter's textual `bigint expires_at` result back to Auth.js's safe-integer contract, and keep `pg` only for transaction-owning maintenance and the isolated PostgreSQL harness, because Neon 1.1 inlines its driver types and this removes the cross-driver Pool cast without weakening per-request closure, OAuth, UUID-text schema, or PostgreSQL 17/18 evidence. Revisit when Auth.js changes the adapter schema or expiry mapping, or Neon changes its request-scoped Pool guidance. → [docs/runbooks/web-auth.md](docs/runbooks/web-auth.md)
- 2026-08-01: Select `intfloat/multilingual-e5-small` at immutable revision `d1d99a1efae6779390caba937d92c54b5bc70e51` as the first dense arm, with exact NumPy/sentence-transformers/Torch/Transformers versions and `torch==2.13.0` from PyTorch's explicit CPU-only index isolated behind an eval-only extra, offline-only cached CPU execution, E5's exact asymmetric prefixes, and an in-memory exact cosine scan instead of a vector database, because the Korean/mixed-script experiment needs a reproducible open baseline without adding CUDA weight, cloud cost, serving-image weight, network behavior, or an index-policy confounder. Revisit the model or vector-store choice only after a retained evaluation result or corpus scale demonstrates a concrete limitation. → [docs/reference/retrieval-methods.md](docs/reference/retrieval-methods.md)
- 2026-08-01: Keep `topic-smoke-v1` behind a two-artifact owner-review boundary: pin the exact sparse/dense/fusion candidate policy in the label-free seed, let registered retrievers create its deterministic blind pending pool only once without overwriting manual progress, and require complete judgements plus pool-completeness attestations to be checksum-sealed against the exact pending bytes in an ancestor Git commit before they can materialize owner-reviewed qrels; bind multi-dataset publication candidates to the exact dataset ID and checksum, and install the dense extra only for an explicitly selected topic publication image, because retrieval output cannot certify its own relevance, agents must not choose different judgement pools, a generic artifact must not be replayed against different gold, and lightweight CI/known-item builds must not inherit Torch. Treat the protected owner-reviewed PR and `Evaluation Publication` approval as the owner authentication boundary because an unsigned local commit is reproducible provenance, not identity proof. Revisit if a signed review UI replaces the JSON workflow or the pooling protocol changes. → [docs/runbooks/topic-qrel-review.md](docs/runbooks/topic-qrel-review.md)
- 2026-08-01: Give each anonymous native runtime an immutable exact `X-Agent-Token-Intent: anonymous` routing intent across bootstrap, expiry and 401 remints, and run-scoped cancellation refreshes, dispatch only that intent before Auth.js, and keep owner runtimes headerless and fail-closed, because public Turnstile and cookie credentials must remain usable during Auth.js, Neon, or OAuth outages without allowing an outage to mint owner scope. Revisit if the token routes split by URL or a server-issued opaque bootstrap envelope replaces client-readable intent. → [docs/runbooks/public-anonymous-chat.md](docs/runbooks/public-anonymous-chat.md)
- 2026-08-01: Add a manual local-only P4.5 adapter fixed to OpenAI Responses `gpt-5.6-luna`, reviewed dependency/pricing identity, exact count-before-generation and response settlement, fresh isolated attempts, explicit paid acknowledgement, and a caller-supplied worst-case generation-token micro-dollar ceiling, while permanently labelling its results `provider-backed-local-unattested` and adding no paid CI or public flag, because real QuickJS × dynamic-subagent quality/cost evidence is useful but a local API-key run cannot authorize anonymous traffic. Require a separate acknowledgement that official input-token-count request pricing is undocumented instead of presenting the generation ceiling as a provider-wide hard cap. Revisit only with owner-reviewed task labels and a signed publication path that preserves the public spend and abuse gates. → [eval/README.md](eval/README.md)
- 2026-08-01: Give the still-disabled anonymous agent three independent process-local ingress budgets—paid commands at 4/identity and 24/global per minute, thread creation at 6/identity and 60/global per hour, and other allowed non-spending routes at 30/identity and 180/global per minute—while accepting only one user text message and binding/executing exactly the six curated retrieval tools, because the paid ledger cannot contain storage/read abuse and hidden Deep Agents schemas or replayed client roles are not authorization boundaries. Revisit when traffic requires a distributed limiter, assistant-ui intentionally adds another reviewed user content part, or the anonymous tool tier changes. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-08-02: Bind every local P4.5 provider run to the task-set's exact published-index content tree, expose only one compiled `evidence-checker`, require that delegation once, and use run schema v5 to record exact root call/completion chronology plus the derived compiled-child count `delegated_tool_calls = tool_calls - quickjs_calls - task_calls`, while scoping generation cost separately from `/responses/input_tokens`, because stale content, unrecorded or extra root tools, hidden child activity, parallel/reordered combined calls, another specialist, or ambiguous billing would invalidate the 2×2 evidence. Revisit if the capability design or provider count-endpoint billing contract changes. → [eval/README.md](eval/README.md)
- 2026-08-02: Pin Luna generation and input counting to the official OpenAI `/v1` root clients and reject every OpenAI-specific ambient host, admin-credential, organization, project, custom-header, and proxy override before reading the separately required `OPENAI_API_KEY`, because SDK environment inheritance could otherwise change the provider evidence identity or override authorization while outer LangChain fields still appeared valid. Revisit only through a reviewed multi-project or alternate-provider routing contract. → [eval/README.md](eval/README.md)
- 2026-08-02: Reduce failed local provider attempts to a fixed phase, allowlisted reason, finalized structural counters, and `eval`/`task`/`other` root-event categories while discarding raw exception chains, prompts, provider bodies, arguments, and call IDs; allow incomplete provider usage only on that non-artifact failure path, because a provider failure has no trustworthy usage payload but must not hide its safe diagnostic behind successful-run accounting or leak secrets through tracebacks. Revisit if signed redacted telemetry replaces local diagnostics. → [eval/README.md](eval/README.md)
- 2026-08-02: Supersede the anonymous ingress portion of the 2026-08-01 entry with the exact outer-to-inner production order `GuestIngressGuard -> NativeThreadGuard -> GuestRunGuard`: charge every authenticated guest request before route/body/database work, charge paid capacity and ledger only after native ownership/status, hold 2/identity and 4/global SSE leases through response cleanup, and serialize stored-thread admission at 6/identity and 256 exact canonical guests under one domain-separated PostgreSQL creation lock held through downstream commit; also pin the unique ordered six-tool guest root surface, inline retrieval prompt, and server-unique checkpoint message IDs with public assistant-ui correlation. This code does not activate public flags or the paused retention Scheduler; recurring GC remains a pre-launch gate. Revisit only with an equally fail-closed distributed ingress/storage admission design or a versioned native Aegra public boundary that preserves these contracts. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-08-02: Keep the native assistant-ui Agent Protocol fixture suite and add a separate real-route Playwright+Axe suite for the signed-out home, login, representative blog desktop/mobile, dark theme, and mobile focus behavior, because the protocol fixture cannot detect site landmarks, contrast, navigation names, or page-level overflow. Revisit if one isolated harness can cover native runtime faults and full Next.js routes without external services. → [web/playwright.site.config.ts](web/playwright.site.config.ts)
- 2026-08-02: Give Preview and Production separate repository-owned anonymous runtime maps while both remain fail-closed, because the eventual paid Production launch must not make Preview inherit its model, budget, or access flag. Revisit only if Preview receives its own independently approved public-test contract. → [docs/runbooks/public-anonymous-chat.md](docs/runbooks/public-anonymous-chat.md)
- 2026-08-03: Configure the production-only guest-retention Scheduler active every 15 minutes under its dedicated invoker after explicit owner approval, while keeping both web public flags disabled until the exact-project plan, apply, and first bounded checkpoint-first execution are verified, because anonymous retention is a launch prerequisite rather than an optional cleanup path. Revisit if retention moves to a durable database-native policy or public chat is retired. → [docs/runbooks/public-anonymous-chat.md](docs/runbooks/public-anonymous-chat.md)
- 2026-08-03: Activate only the repository-owned Production guest tuple at `openai:gpt-5.6-luna`, 500,000 µUSD per UTC day and 6,892 µUSD per run, with `OPENAI_API_KEY` pinned to a positive numeric Secret Manager version while Preview remains disabled and OpenAI-free, because the owner approved the paid generation ceiling and partial or apply-time activation would bypass the reviewed cost boundary. This records desired state only; final public issuance still requires the active Scheduler's exact apply and first bounded execution, count-endpoint billing, Turnstile, Vercel, exact-project plan, and browser gates. Revisit if pricing, provider routing, the approved ceiling, or Preview policy changes. → [docs/runbooks/public-anonymous-chat.md](docs/runbooks/public-anonymous-chat.md)
- 2026-08-03: Supersede the Production per-run reservation in the preceding entry with 18,892 µUSD while keeping the 500,000 µUSD UTC-day ceiling: retain the 12,000-token generation ledger and its 6,892 µUSD worst allocation, and add a separate 48,000-token aggregate count-risk ledger priced at Luna's highest $0.25/M input bucket (12,000 µUSD). Before each `/responses/input_tokens` call, atomically reserve the canonical-payload heuristic `U` in count risk and 1,024 output tokens in generation; a valid exact `n <= U` plus payload parity settles risk from `U` to `n` and charges `n` to generation, while count errors, cancellation, `n > U`, parity drift, or generation-ledger admission failure retain `U` and exhaust the run. This is conservative repository accounting, not a provider hidden-token bound, documented count price, or provider hard stop; input-count billing evidence and a provider-enforced account stop remain launch gates. Reject both old 6,892 and 8,868 tuples in startup, Terraform, and delivery verification; Preview remains disabled and OpenAI-free. Revisit when OpenAI documents count billing, Luna pricing changes, or the guest token policy changes. → [docs/runbooks/public-anonymous-chat.md](docs/runbooks/public-anonymous-chat.md)
- 2026-08-03: Supersede the 2026-07-29 blanket non-operational foundation-live decision with an explicit read-only gate for direct state in exactly `festive-ally-503605-v7` followed by exact-repository GitHub environment governance, while never following organization/folder/ancestor/project-parent scopes or querying another project and retaining unsigned v1 only as an optional unauthenticated structure diagnostic, because direct resource drift can be verified without pretending to prove company-admin provenance, project parentage, inherited IAM completeness, public-launch readiness, or bounded spend. Require a trusted local workstation/shell/checkout premise, current-user/root-owned non-group/other-writable toolchain ancestry, passwd-derived home, sanitized child environments, and a SHA-pinned literal command oracle; this does not claim resistance to a malicious same-user workstation or loader activity before the initial shell. Permission/API failures are stop conditions, not authority to grant IAM, enable APIs, attach billing, change settings, or execute jobs. Require separately signed company-admin evidence only if broader inherited-policy claims later become necessary. → [docs/runbooks/gcp-neon-foundation.md](docs/runbooks/gcp-neon-foundation.md)
- 2026-08-10: Keep the home chat conversation-first and move thread history and run inspection into on-demand sheets at every viewport, because the fixed three-column lab layout obscured the visitor's question flow. Revisit if measured desktop usage shows a persistent navigation or inspection need. → [docs/adr/0005](docs/adr/0005-adopt-assistant-ui.md)
- 2026-08-10: Supersede the uv 0.11.29 toolchain baseline with uv 0.12.3 across workspace, workflows, checksums, governance tests, and immutable images while refreshing the agent Python 3.12 slim-trixie digest, because resolver and container updates must remain one reviewed reproducible unit. Revisit when the next uv or Python runtime update is accepted. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-08-10: Refresh the reproducible CI and protocol toolchain to Node 24.19.0, PostgreSQL 17.10, and actions/attest 4.2.2 while retaining TypeScript 5.9.3, because exact runtime pins must move with their protocol fixtures, workflow baselines, and mutation tests. Revisit on the next coordinated runtime pin update.
- 2026-08-10: Refresh the reproducible CI and protocol toolchain to Node 24.19.0 with its Corepack 0.35.0 release, PostgreSQL 17.10, and actions/attest 4.2.2 while retaining TypeScript 5.9.3, because exact runtime pins must move with their protocol fixtures, workflow baselines, and mutation tests. Revisit on the next coordinated runtime pin update.
- 2026-08-10: Supersede the Terraform 1.13.5 baseline with Terraform 1.15.8 and hashicorp/google 7.43.0 while retaining the credential-free mock and preflight contracts, because runtime and provider pins must advance with their lockfile and exact static verification. Revisit on the next coordinated Terraform stack update.
- 2026-08-11: Supersede the 2026-07-27 three-method merge policy by allowing only rebase merges in repository settings and the main ruleset, because GitHub has no separate default merge-method setting and parallel work should retain linear history. Revisit if GitHub adds a native default-method control or the repository adopts a merge queue that requires another method. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-08-14: Supersede the 2026-08-03 `openai:gpt-5.6-luna / 500000 / 18892` guest tuple with `openai:gpt-5.6-luna / 500000 / 19892`: allow up to 8 model calls, 512 output tokens per call, 16,000 generation tokens per run, and 48,000 aggregate count-risk tokens, which yields 7,892 µUSD generation plus 12,000 µUSD count risk; open the existing server-declared, stateless dynamic specialists to canonical guests without login or a separate one-task ceiling, while signed-in `model:select` remains limited to GPT-5.6 Luna, Terra, or Sol and guests remain Luna-pinned. Revisit if spend evidence requires tighter limits, a model leaves the reviewed Responses contract, or another provider is proposed. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-08-14: Supersede the same-day `openai:gpt-5.6-luna / 500000 / 19892` guest tuple with `openai:gpt-5.6-luna / 500000 / 47892`: production evidence showed that the former 16,000 generation-token and 48,000 count-risk limits rejected the final root synthesis after a valid specialist run, so permit 48,000 generation tokens, 128,000 count-risk tokens, and 24 read-only tool calls while retaining 8 model calls, 512 output tokens per call, 45 seconds, and the 500,000 µUSD daily ceiling. Accept both standalone and whitespace-separated inline task-envelope headings because Luna emits both forms and the same ordered, unique, non-empty server validation remains enforced. Revisit if measured successful traces support lower reservations or provider billing changes. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-08-14: Supersede the same-day task-heading parser with prompt-guided stateless headings while retaining exact task arguments, UTF-8 and 16 KB limits, server-declared specialists, shared budgets, depth one, and read-only child tools, because Luna may add valid Markdown around headings and generated prose formatting is not an authorization boundary. Revisit if task descriptions become client-authored or a typed delegation schema replaces model prose. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-08-14: Preload the fixed server-owned blog retrieval skill through each child's isolated `SkillsMiddleware` prompt and remove its redundant model tool roundtrip, because a production-parity anonymous delegation used all eight model calls before final synthesis. Retain the isolated skill backend, fixed specialist tools, shared budgets, and depth-one boundary. Revisit if child skills become dynamic or grow enough to require progressive loading. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-08-14: Keep the anonymous hard output ceiling at 512 tokens but ask the root to complete its visitor-facing answer within 400, because the fixed subagent path completed in seven model calls while a verbose comparison still reached the response ceiling. Revisit if measured answers remain incomplete or the guest cost tuple changes. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-08-14: Supersede the same-day 48,000-token, 47,892 µUSD guest run tuple with a 64,000-token generation ledger and 51,892 µUSD reservation while retaining eight model calls, 512 output tokens per call, 128,000 count-risk tokens, 45 seconds, and the 500,000 µUSD daily ceiling, because a production-parity Luna delegation succeeded at 39,619 tokens but another valid stochastic path exhausted 48,000. Revisit after retained successful traces establish a tighter reliable ceiling or provider pricing changes. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-08-14: Supersede the same-day 51,892 µUSD guest run tuple with 53,837 µUSD and raise the per-call output ceiling from 512 to 768 tokens while retaining the 64,000-token generation ledger, 128,000 count-risk ledger, eight model calls, 45 seconds, and 500,000 µUSD daily ceiling, because the deployed anonymous specialist completed its tools but Luna ended the root comparison mid-table at the prior ceiling. Require every OpenAI response to report a complete terminal state so partial provider output fails closed instead of appearing successful. Revisit after retained complete-answer traces establish a lower reliable ceiling or provider pricing changes. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-08-15: Supersede the 2026-07-26 and 2026-07-28 Turnstile contracts with Vercel BotID Basic, and supersede the 2026-08-02 and 2026-08-03 pre-launch Scheduler, retention, input-count, provider-stop, and browser gates with an explicit post-launch evidence posture: Production anonymous chat is live at `syshin0116.vercel.app`, with Luna fixed for guests, server-declared dynamic specialists, 14-day guest retention, and the 500,000/53,837 µUSD application budgets while Preview remains closed; the unretained evidence remains urgent operational risk rather than proof that the implementation or live access is absent. Revisit if public access closes or the model, capability, retention, cost, or risk-acceptance policy changes. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)
- 2026-08-15: Supersede the 2026-07-28 agent delivery contract with the current native release, which builds an immutable digest without provenance or SBOM, requires current-main CI and owner approval, verifies `/live`, `/ready`, and unauthenticated APv2 `401` before 100% traffic promotion, and keeps both Agent environments at zero secrets and variables; database jobs, authenticated provider smoke, and rollback remain separately approved manual operations because the current workflows do not implement the former token-backed chain. Revisit if those operations return to repository automation. → [docs/runbooks/cloud-run-delivery](docs/runbooks/cloud-run-delivery.md)
- 2026-08-15: Supersede the 2026-07-28 two-environment Cloud Run and three-stage Terraform decisions by provisioning Cloud Run resources only for Production, retaining Preview as a dormant foundation and runtime map without a service or job, and using `foundation -> jobs -> services -> launch`; keep `launch` on every later full-root plan because returning to `services` would plan removal of the protected Scheduler. Revisit if Preview resources are restored or the Scheduler leaves this Terraform root. → [docs/runbooks/cloud-run-delivery](docs/runbooks/cloud-run-delivery.md)
- 2026-08-18: Delegate chat answer markdown to assistant-ui's `MarkdownTextPrimitive` with `memoizeMarkdownComponents` instead of wiring `react-markdown` directly in `chat-shell.tsx`, because the hand-rolled renderer overrode only `a`, `code`, and `pre` while every other tag fell back to Tailwind preflight resets that the `app/globals.css` markdown styles cannot reach from their `.prose` scope. Syntax highlighting stays out because assistant-ui leaves it off by default and the existing `shiki` code block is pinned to a light theme. Revisit if the chat needs rendering the upstream primitive does not cover, or if `@assistant-ui/react-streamdown` replaces it to bundle Shiki, KaTeX, and Mermaid. → [docs/adr/0005](docs/adr/0005-adopt-assistant-ui.md)
- 2026-08-20: Bind the Production Cloud Run agent to LangSmith by mounting `langsmith-api-key` as `LANGCHAIN_API_KEY` and setting `LANGCHAIN_TRACING_V2=true` with `LANGCHAIN_PROJECT="syshin0116's dev (GCP)"`, superseding the 2026-08-15 posture that left LangSmith an unbound Production container; the exact runtime tuple grows to 22 environment entries and five numeric delivery secret pins, and Preview stays untraced while it is dormant, because a live public agent whose runs cannot be inspected is diagnosed from Cloud Run request logs alone, which record neither model output nor tool activity. Anthropic remains a versionless container. Revisit if Preview gains its own traced runtime or the tracing project changes. → [docs/runbooks/gcp-neon-foundation.md](docs/runbooks/gcp-neon-foundation.md)
- 2026-08-20: Send Production LangSmith traces synchronously with `LANGCHAIN_CALLBACKS_BACKGROUND=false`, growing the exact runtime tuple to 23 environment entries, because Cloud Run runs the service with `cpu_idle=true` and the SDK's background batch flush is scheduled after the response, when the instance holds no CPU; the first traced revision produced zero runs and never created its project. Accept the per-request latency of flushing inside the request as the cost of having any trace at all. Revisit if the service stops throttling idle CPU or the SDK gains a request-bound flush. → [docs/runbooks/gcp-neon-foundation.md](docs/runbooks/gcp-neon-foundation.md)
- 2026-08-20: Let Terraform own Cloud Run traffic by dropping `traffic` from the service's `ignore_changes`, so the already-declared `TRAFFIC_TARGET_ALLOCATION_TYPE_LATEST` actually takes effect and a configuration apply promotes the revision it creates; the image stays ignored so the release workflow keeps owning which digest ships. The previous split silently left production on a two-day-old revision through two applies, because Terraform created revisions it was forbidden to route to, and nothing else promotes a configuration-only change. Routing all traffic to the latest revision is Cloud Run's documented continuous-deployment pattern, and the existing `/ready` startup probe is the gate: a revision that never becomes ready never serves and the previous one keeps traffic. Revisit if progressive delivery requires holding traffic on a named revision. → [docs/runbooks/cloud-run-delivery.md](docs/runbooks/cloud-run-delivery.md)
- 2026-08-20: Derive corpus size from the current published snapshot in builds and assert artifact consistency instead of pinning `335` across CI, Docker, tests, and runbooks, because the repeated absolute count made every post addition require code changes while the content tree SHA and corpus fingerprint already bind reproducibility. Revisit if an external publication contract requires a separately reviewed absolute count. → [eval/README.md](eval/README.md)
- 2026-08-20: Supersede manually copied Agent and Eval workflow AST constants with one generated baseline file and an update command, because digest calculation is mechanical while approval belongs in review of the workflow and baseline diff together. Revisit if workflow approval moves to a separately signed policy. → [docs/reference/github-governance.md](docs/reference/github-governance.md)
- 2026-08-22: Raise the signed-in output ceiling from 2,048 to 128,000 tokens under a named `owner-capability-lab-v5` policy in `agent/graph.py`, leaving the library default in `capabilities/budget.py` untouched, because a truncated provider response is discarded whole by the usage contract - a live answer hit `incomplete_details.reason=max_output_tokens` at exactly 2,048 and the visitor received nothing at all. The owner run already had `sys.maxsize` total and count-risk limits, so the per-response ceiling was the only bound; the effective limits are now the 60-second model timeout and the 90-second run budget. Guests keep their far lower reviewed ceiling because they spend from the public budget. Revisit if discarding incomplete responses is replaced by surfacing the partial answer, which would make the ceiling a display concern rather than a failure. → [docs/adr/0006](docs/adr/0006-public-anonymous-chat-access.md)

- 2026-09-07: Upgrade the agent to native Deep Agents dynamic subagents and Aegra lifecycle handling first; retain legacy guest command compatibility so the standard frontend runtime can follow after the server release (PR #260).
